# Provision-only local mode. Populate these values from the selected azd environment.
APIM_PROJECT_ENDPOINT="https://<apim-name>.azure-api.net/ai-gateway/api/projects/<project>"
AZURE_AI_MODEL_DEPLOYMENT_NAME="<deployment-name>"
TOOLBOX_ENDPOINT="https://<apim-name>.azure-api.net/tool-<project-name>-mcp"
