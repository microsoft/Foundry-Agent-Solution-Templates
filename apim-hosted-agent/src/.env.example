# Provision-only local mode. Populate these values from the selected azd environment.
AGENT_APIM_PROJECT_ENDPOINT="https://<apim-name>.azure-api.net/ai-gateway/api/projects/<project>"
AGENT_MODEL_DEPLOYMENT="<deployment-name>"
AGENT_TOOLBOX_ENDPOINT="https://<apim-name>.azure-api.net/tool-<project-name>-mcp"
