<policies>
  <inbound>
    <base />
    <validate-azure-ad-token
      tenant-id="{{foundry-tenant-id}}"
      header-name="Authorization"
      output-token-variable-name="agent-token"
      failed-validation-httpcode="401"
      failed-validation-error-message="A valid hosted-agent identity token is required.">
      <client-application-ids>
        <application-id>{{foundry-agent-principal-id}}</application-id>
      </client-application-ids>
      <audiences>
        <audience>https://ai.azure.com</audience>
      </audiences>
      <required-claims>
        <claim name="oid" match="any">
          <value>{{foundry-agent-principal-id}}</value>
        </claim>
      </required-claims>
    </validate-azure-ad-token>
    <set-variable name="callerObjectId" value='@(((Jwt)context.Variables["agent-token"]).Claims.GetValueOrDefault("oid", new string[] { "unknown" })[0])' />
    <set-variable name="tokenlimit-{{foundry-model-deployment-name}}" value="{{foundry-model-token-limit}}" />
    <set-variable name="tokenquota-{{foundry-model-deployment-name}}" value="{{foundry-model-token-quota}}|{{foundry-model-token-quota-period}}" />
    <set-backend-service backend-id="{{foundry-backend-id}}" />
    <choose>
      <when condition='@(context.Variables.ContainsKey("tokenlimit-" + (string)context.Request.Foundry.Deployment) || context.Variables.ContainsKey("tokenquota-" + (string)context.Request.Foundry.Deployment))'>
        <set-variable name="deploymentName" value="@((string)context.Request.Foundry.Deployment)" />
        <set-variable name="counterKey" value='@("project/{{foundry-project-name}}/principal/" + (string)context.Variables["callerObjectId"] + "/deployment/" + (string)context.Variables["deploymentName"])' />
        <set-variable name="limitVariableName" value='@("tokenlimit-" + (string)context.Variables["deploymentName"])' />
        <set-variable name="quotaVariableName" value='@("tokenquota-" + (string)context.Variables["deploymentName"])' />
        <set-variable name="tokenLimitValue" value='@((string)context.Variables[(string)context.Variables["limitVariableName"]])' />
        <set-variable name="tokenQuotaValue" value="@(((string)context.Variables[(string)context.Variables[&quot;quotaVariableName&quot;]]).Split('|')[0])" />
        <set-variable name="tokenQuotaPeriod" value="@(((string)context.Variables[(string)context.Variables[&quot;quotaVariableName&quot;]]).Split('|')[1])" />
        <choose>
          <when condition='@(((string)context.Variables["tokenQuotaPeriod"]) == "Hourly")'>
            <llm-token-limit counter-key='@((string)context.Variables["counterKey"])' tokens-per-minute='@(int.Parse((string)context.Variables["tokenLimitValue"]))' token-quota='@(long.Parse((string)context.Variables["tokenQuotaValue"]))' token-quota-period="Hourly" estimate-prompt-tokens="false" />
          </when>
          <when condition='@(((string)context.Variables["tokenQuotaPeriod"]) == "Daily")'>
            <llm-token-limit counter-key='@((string)context.Variables["counterKey"])' tokens-per-minute='@(int.Parse((string)context.Variables["tokenLimitValue"]))' token-quota='@(long.Parse((string)context.Variables["tokenQuotaValue"]))' token-quota-period="Daily" estimate-prompt-tokens="false" />
          </when>
          <when condition='@(((string)context.Variables["tokenQuotaPeriod"]) == "Weekly")'>
            <llm-token-limit counter-key='@((string)context.Variables["counterKey"])' tokens-per-minute='@(int.Parse((string)context.Variables["tokenLimitValue"]))' token-quota='@(long.Parse((string)context.Variables["tokenQuotaValue"]))' token-quota-period="Weekly" estimate-prompt-tokens="false" />
          </when>
          <when condition='@(((string)context.Variables["tokenQuotaPeriod"]) == "Monthly")'>
            <llm-token-limit counter-key='@((string)context.Variables["counterKey"])' tokens-per-minute='@(int.Parse((string)context.Variables["tokenLimitValue"]))' token-quota='@(long.Parse((string)context.Variables["tokenQuotaValue"]))' token-quota-period="Monthly" estimate-prompt-tokens="false" />
          </when>
          <otherwise>
            <llm-token-limit counter-key='@((string)context.Variables["counterKey"])' tokens-per-minute='@(int.Parse((string)context.Variables["tokenLimitValue"]))' token-quota='@(long.Parse((string)context.Variables["tokenQuotaValue"]))' token-quota-period="Yearly" estimate-prompt-tokens="false" />
          </otherwise>
        </choose>
      </when>
      <otherwise />
    </choose>
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <set-header name="x-keyless-model-gateway" exists-action="override">
      <value>true</value>
    </set-header>
    <base />
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>